- какой индекс у первого элемента массива arr? - ...[0]
- как выглядит первый элемент массива arr? - ...arr[0]
- какой индекс у последнего элемента массива myArray? - myArray.lenth -1...
- как выглядит последний элемент массива numbers? - number[numbers.length -1]
- как выглядит текущий элемент массива word, обрабатываемый (используемый) в цикле? - word[i]
- как выглядит следующий за текущим элемент массива array? - ...array[i+1]
- как выглядит предыдущий перед текущим элемент массива array? - ...array[i-1]


int[] myArrayBalances = ArrayMethods.fillArray(1000, 10000,3);
        ArrayMethods.printArray(myArrayBalances);

        Scanner scanner = new Scanner(System.in);
        System.out.println("Input the interest rate on the account balance: ");
        int n = scanner.nextInt();

                int[] tempArray = new int[myArray.length];
                int count = 0;
                int[] elementsArray = new int [count];

                for (int i = 0; i < elementsArray.length; i++) {
                    for (int j = 0; j < myArray.length; j++) {
                        if (myArray[i] != elementsArray[i]){
                            elementsArray[i] = myArray[i];
                            count++;
                        }
                    }
                    return elementsArray;
                }






               /* for (int i = 0; i < elementsArray.length; i++) {
                    for (int j = 0; j < myArray.length; j++) {
                        if (elementsArray[i] == myArray[j]) {
                            count[i]++;

                        }
                    }
                }
            }
        */